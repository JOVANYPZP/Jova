#include <stdio.h>

int main() {
    float c;
    printf("Ingresa la calificación: ");
    scanf("%f", &c);
    
    if (c >= 6) {
        printf("APROBADO\n");
        puts("APROBADO");
    } else {
    }

    return 0;
}
